name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'GitHub Release tag name, for example v1.2.3; build source always comes from main'
        required: true
        type: string

jobs:
  # Skip semver pre-release tags (e.g. v1.0.0-rc1); those are handled by pre-release.yaml.
  build:
    if: ${{ github.event_name == 'workflow_dispatch' || !contains(github.ref_name, '-') }}
    uses: ./.github/workflows/_build-wheel.yaml
    with:
      runner: ubuntu-22.04
      container: pytorch/manylinux2_28-builder:cuda12.8
      cuda: container
      build-with-ep: '1'
      torch-cuda-arch-list: '8.0;9.0'
      ep-torch-versions: '2.11.0;2.12.0;2.12.1'
      build-nvlink-allocator: true
      source-ref: ${{ github.event_name == 'workflow_dispatch' && 'main' || '' }}
      release-version: ${{ inputs.release_tag || github.ref_name }}
      cmake-args: >-
        -DBUILD_UNIT_TESTS=OFF -DUSE_HTTP=ON -DUSE_ETCD=ON -DUSE_CUDA=ON
        -DWITH_EP=ON -DSTORE_USE_ETCD=ON -DENABLE_SCCACHE=ON -DCMAKE_BUILD_TYPE=Release
      artifact-prefix: mooncake-wheel

  build-arm64:
    if: ${{ github.event_name == 'workflow_dispatch' || !contains(github.ref_name, '-') }}
    uses: ./.github/workflows/_build-wheel.yaml
    with:
      runner: ubuntu-22.04-arm
      container: pytorch/manylinuxaarch64-builder:cuda12.8
      cuda: container
      cmake-generator: Ninja
      torch-cuda-arch-list: '9.0'
      source-ref: ${{ github.event_name == 'workflow_dispatch' && 'main' || '' }}
      release-version: ${{ inputs.release_tag || github.ref_name }}
      cmake-args: >-
        -DBUILD_UNIT_TESTS=OFF -DUSE_HTTP=ON -DUSE_CUDA=ON -DUSE_MNNVL=ON -DWITH_EP=OFF
        -DWITH_STORE_RUST=OFF -DENABLE_SCCACHE=ON -DCMAKE_BUILD_TYPE=Release
      artifact-prefix: mooncake-wheel-arm64

  publish-release:
    needs: [build, build-arm64]
    permissions:
      contents: write
      id-token: write
    uses: ./.github/workflows/_publish-wheel.yaml
    with:
      artifact-pattern: 'mooncake-wheel*'
      release-tag: ${{ inputs.release_tag || github.ref_name }}
      target-commitish: ${{ github.event_name == 'workflow_dispatch' && 'main' || github.sha }}
    secrets:
      pypi-token: ${{ secrets.PYPI_API_TOKEN }}
